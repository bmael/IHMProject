INCLUDEPATH += . ..
WARNINGS += -Wall

# The following keeps the generated files at least somewhat separate 
# from the source files.
UI_DIR = .cmp/ui
MOC_DIR = .cmp/moc
OBJECTS_DIR = .cmp/obj
RCC_DIR = .cmp/rcc
